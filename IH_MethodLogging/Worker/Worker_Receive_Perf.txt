  //%attributes = {"invisible":true,"preemptive":"capable","lang":"en"} comment added and reserved by 4D.
  // Worker_Receive_Perf (performanceArrayObj)
  // Worker_Receive_Perf (object)
  // 
  // DESCRIPTION
  //   
  //
C_OBJECT($1;$obj)
  // ----------------------------------------------------
  // HISTORY
  //   Created by: DB (05/23/2017)
  // ----------------------------------------------------

ASSERT(Count parameters=1)
$obj:=$1

Init_ThreadSafe 

ARRAY TEXT($at_method;0)
ARRAY LONGINT($al_minTime;0)
ARRAY LONGINT($al_maxTime;0)
ARRAY REAL($ar_totalTime;0)
ARRAY LONGINT($al_count;0)

If (OB Is defined($obj;"methods"))
OB GET ARRAY($obj;"methods";$at_method)
OB GET ARRAY($obj;"minTime";$al_minTime)
OB GET ARRAY($obj;"maxTime";$al_maxTime)
OB GET ARRAY($obj;"totalTime";$ar_totalTime)
OB GET ARRAY($obj;"count";$al_count)
End if 

C_LONGINT($pos)
For ($i;1;Size of array($at_method))
$pos:=Find in array(_ML_Perf_Method;$at_method{$i})
If ($pos<1)  // not found
APPEND TO ARRAY(_ML_Perf_Method;$at_method{$i})
APPEND TO ARRAY(_ML_Perf_minTime;$al_minTime{$i})
APPEND TO ARRAY(_ML_Perf_maxTime;$al_maxTime{$i})
APPEND TO ARRAY(_ML_Perf_totalTime;$ar_totalTime{$i})
APPEND TO ARRAY(_ML_Perf_count;$al_count{$i})
Else 

If (_ML_Perf_minTime{$pos}>$al_minTime{$i})
_ML_Perf_minTime{$pos}:=$al_minTime{$i}
End if 

If (_ML_Perf_maxTime{$pos}<$al_maxTime{$i})
_ML_Perf_maxTime{$pos}:=$al_maxTime{$i}
End if 

_ML_Perf_totalTime{$pos}:=_ML_Perf_totalTime{$pos}+$ar_totalTime{$i}
_ML_Perf_count{$pos}:=_ML_Perf_count{$pos}+$al_count{$i}
End if 
End for 
